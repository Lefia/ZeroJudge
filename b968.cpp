#include<bits/stdc++.h>
using namespace std;

int main()
{
    string a, b;
    while(getline(cin,a))
    {
        getline(cin,b);
        cout << a<< " and " << b << " sitting in the tree" <<endl;
    }
}